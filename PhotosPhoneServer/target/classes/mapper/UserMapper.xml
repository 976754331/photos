<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hoperun.mapper.UserMapper">
  <resultMap id="UserResultMap" type="com.hoperun.model.po.User">
     <id column="user_id" jdbcType="VARCHAR" property="id" />
     <result column="name" jdbcType="VARCHAR" property="name" />
     <result column="sex" jdbcType="VARCHAR" property="sex" />
     <result column="url" jdbcType="VARCHAR" property="url" />
     <result column="email" jdbcType="VARCHAR" property="email" />
     <result column="create_time" jdbcType="VARCHAR" property="createTime" />
  </resultMap>
  <select id="getUserInfo" parameterType="java.lang.String" resultMap="UserResultMap">
     select user_id, name, sex, url, email, create_time
     from tb_user_info
     where user_id = #{userId,jdbcType=VARCHAR}
  </select>
  <select id="login" parameterType="map" resultType="map">
     select user_id
     from tb_user_qrcode
     where login_name = #{userName}
     and login_pwd = #{pwd}
  </select>
  
  <!-- 增加意见建议记录 -->
  <insert id="insertSuggest" parameterType="map">
  	 insert into tb_suggest (suggest_id,person_id,suggest_proposalcontent,suggest_proposaldate)
  	  values(#{suggestId},#{personId},#{suggestion},#{date}) 
  </insert>
  
  <update id="updateUserInfo" parameterType="com.hoperun.model.po.User">
     update tb_user_info
     <set >
       <if test="name != null">
           name = #{name,jdbcType=VARCHAR},
       </if>
       <if test="sex != null">
           sex = #{sex,jdbcType=VARCHAR},
       </if>
        <if test="url != null">
           url = #{url,jdbcType=VARCHAR},
       </if>
        <if test="email != null">
           email = #{email,jdbcType=VARCHAR}
       </if>
     </set>
     where user_id = #{id,jdbcType=VARCHAR}
  </update>
  <insert id="insert" parameterType="com.hoperun.model.po.User" >
    insert into tb_user_info
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        user_id,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="sex != null" >
        sex,
      </if>
      <if test="email != null" >
        email,
      </if>
      <if test="url != null" >
        url,
      </if>
      <if test="createTime != null" >
        create_time,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id,jdbcType=VARCHAR},
      </if>
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="sex != null" >
        #{sex,jdbcType=VARCHAR},
      </if>
      <if test="email != null" >
        #{email,jdbcType=VARCHAR},
      </if>
      <if test="url != null" >
        #{url,jdbcType=VARCHAR},
      </if>
      <if test="createTime != null" >
        #{createTime,jdbcType=VARCHAR},
      </if>
      
    </trim>
  </insert>
  
  
  
  
  

</mapper>